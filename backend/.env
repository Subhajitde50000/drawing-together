# Backend environment configuration

# Server
HOST=0.0.0.0
PORT=8000

# CORS — comma-separated list of allowed frontend origins
# Example: http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=https://drawing-togethers.netlify.app

# Max players per room (default 2)
MAX_PLAYERS_PER_ROOM=2

# FRONTEND URL so we can add to CORS when deployed
#FRONTEND_URL=https://drawing-togethers.netlify.app
